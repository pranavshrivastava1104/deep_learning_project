# Unit tests

Unit tests in this directory must be fast, deterministic, and independent of network access, large datasets, model downloads, GPUs, Redis, PostgreSQL, and MLflow.

Integration with those dependencies belongs in later integration or end-to-end test suites.
