# Notebooks

Notebooks are reviewable launchers and explanations, not the source of reusable project logic. They must import committed Python modules from this repository.

Notebook outputs should remain small. Datasets, checkpoints, exported models, credentials, and large generated results must not be committed.
